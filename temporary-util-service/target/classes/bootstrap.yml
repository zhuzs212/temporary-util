#server:
#  port: 8888
spring:
  application:
    name: consumer  #微服务的名称
#  profiles:
#    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        file-extension: yml
        group: DEFAULT_GROUP
#        # 指定要拉取的配置文件名（dataId）
#        shared-configs:
#          - dataId: consumer.yml
#            group: DEFAULT_GROUP
#            refresh: true


